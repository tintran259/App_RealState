const initialState = {
   listUser: [
      {
         id: 1,
         username: "admin",
         password: "abc",
      }
   ]
}

const reducer = (state = initialState, action) => {
   return state
}

export default reducer 